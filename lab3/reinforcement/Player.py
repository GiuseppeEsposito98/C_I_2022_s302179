from Nim import *

class Player:
    def __init__(self, nim, alpha=0.1, random_factor=0.4):
        self.nim = nim
        self.alpha = alpha
        self.random_factor = random_factor
        self.state_history = [(nim, 0)]
        self.G = dict()

    def initial_status(self):
        for i in nim.

    

